version: "3"
services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    environment:
      - MODE=development
    ports:
      - "80:8080"
      - "8080:8080"
    networks:
      - default
